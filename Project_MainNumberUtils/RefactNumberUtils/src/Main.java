// MainRefactNumberUtils
public class MainRefactNumberUtils {
    // Método estático para verificar se i numero é impar
    public static boolean isOdd(int number) {
        return number % 2 != 0; // Mantém a lógica do método original.

                // Método estático para verificar se o numero é positivo
            public static boolean   isPositive; (int number) {
                    return  number > 0; // Um numero positivo se for maior que zero
        }
    }
}
